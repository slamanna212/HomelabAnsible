# Docker Image Cleanup - Remove unused images
# Runs daily at 3:15 AM (low-traffic period)
15 3 * * * root /usr/bin/docker image prune -a --filter "until=24h" --force >> /var/log/docker-cleanup.log 2>&1
