[defaults]
# Inventory configuration
inventory = inventory.proxmox.yml

# Shared roles directory, so playbooks under *Lifecycle/ and *Utility/ can
# also resolve roles defined at the repo root (not just root-level playbooks)
roles_path = roles

# Connection settings
remote_user = ansible
private_key_file = ~/.ssh/id_ed25519

# Other settings
host_key_checking = False
retry_files_enabled = False
interpreter_python = auto_silent

[inventory]
enable_plugins = community.proxmox.proxmox

# Caching disabled to ensure inventory always reflects current Proxmox state
cache = no
# cache_plugin = jsonfile
# cache_timeout = 1800
# cache_connection = /tmp/ansible_inventory_cache

[ssh_connection]
pipelining = True
